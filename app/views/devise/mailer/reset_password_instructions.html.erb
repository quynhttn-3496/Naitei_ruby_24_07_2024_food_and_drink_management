<p><%= t("password_reset.hello", email: @resource.email) %></p>

<p><%= t("password_reset.request") %></p>

<p><%= link_to t("password_reset.change_link"), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t("password_reset.ignore") %></p>
<p><%= t("password_reset.no_change") %></p>
